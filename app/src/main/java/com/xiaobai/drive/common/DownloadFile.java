package com.xiaobai.drive.common;

import com.xiaobai.drive.service.DownloadServiceImpl;

public class DownloadFile {
    /**
     * 请求服务器，检查版本是否可以更新
     *
     */
    public static void checkVersion(final DownloadServiceImpl downloadService) {
        //从网络请求获取到的APK下载路径，此处是随便找的链接
        //downloadService.bindService();
    }
}
